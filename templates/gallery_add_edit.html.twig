{% extends "crm/base_add_edit.html.twig" %}

{% block optional %}
    {% if form.vars.data.filename is not empty %}
        <img src="{{ asset('uploaded/gallery/thumbnail/' ~ form.vars.data.filename) }}" />
    {% endif %}
{% endblock %}